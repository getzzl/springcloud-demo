server:
  port: 10086
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:10086/eureka/
    register-with-eureka: false
    fetch-registry: false
spring:
  application:
    name: eureka-server  #服务名，serviceId
  main:
    allow-bean-definition-overriding: true #当遇到同样名字的时候，是否允许覆盖注册